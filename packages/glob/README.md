# @koopa-cli/glob

```shell
npx @koopa-cli/glob less
```

- Patterns like this are supported:

```shell
npx @koopa-cli/glob less css
npx @koopa-cli/glob less,css
npx @koopa-cli/glob {less,css}
npx @koopa-cli/glob "*.{less,css}"
```

